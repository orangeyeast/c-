#include <stdio.h>

void clear_input_buffer(void) {
	int c;
	while ((c = getchar()) != '\n' && c != EOF) {
		; // 什么都不做，只是把字符丢掉
	}
}

int main(void) {

	int input = 0;

	while (1) {

		printf("please type a number:");

		int result= scanf_s("%d", &input);

		if (result == 1) {
			if (input>=1) {

				break;
			}

			else {

				printf("please type again, the input must be a number larger than 1 or equal to 1\n");

				continue;
			}
		}
		else {

			printf("please type again, the input must be a number!\n");

			clear_input_buffer();

			continue;

		}

	}

	int trigger = 0;

	for (int one=0; one <= input * 10 / 1; one++) {

		for (int two=0; two <= input * 10 / 2; two++) {
			
			for (int five=0; five <= input * 10 / 5; five++ ) {

				if (one*1+two*2+five*5==input*10) {

					printf("%d yuan can divided into %d one jiao, %d two jiao, %d five jiao", input, one, two, five);

					trigger = 1;

					break;
				}
			}

			if (trigger == 1) {

				break;
			}
		}

		if (trigger == 1) {

			break;
		}
	}

	return 0;

}

